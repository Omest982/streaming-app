server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stream_app
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
